{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<div class="d-flex flex-row flex-fill">
    <div class="d-flex flex-column w-25 bg-danger flew-fill" id="sidebar">
        opop
    </div>
    <div class="d-flex flex-column w-75 bg-light" id="content">
        {{ render(controller(
            'App\\Controller\\SkillController::index')) }}
    </div>
    </div>
</div>


{% endblock %}
