{% extends 'base.html.twig' %}

{% block title %}New Task{% endblock %}

{% block body %}
     {% form_theme form 'bootstrap_4_layout.html.twig' %}

<div class="container cust-form-edit">
    <h1>Créer un nouveau  projet  / une nouvelle tache </h1>

    {{ include('task/_form.html.twig') }}
</div>
    <div class="d-flex flex-column justify-content-between">
        <a href="{{ path('admin_task') }}">Retour a la liste des taches</a>
        <a href="{{ path('home_index') }}">Retour au Cv</a>
    </div>
</div>
{% endblock %}
